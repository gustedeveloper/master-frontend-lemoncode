import { Typography } from "@mui/material";
import { FC } from "react";

export const Header: FC = () => (
  <>
    <Typography variant="h3" sx={{ p: " 15px 30px", color: "#cad2c5" }}>
      Lemonstock
    </Typography>
  </>
);
