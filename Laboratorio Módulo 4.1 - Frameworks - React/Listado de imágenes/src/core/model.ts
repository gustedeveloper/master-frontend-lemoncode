export interface PictureInfo {
  id: string;
  picUrl: string;
  title: string;
  price: number;
}

export interface PictureInfoVm {
  id: string;
  picUrl: string;
  title: string;
  price: number;
  selected: boolean;
}

export interface PictureInfoVm {
  id: string;
  picUrl: string;
  title: string;
  price: number;
  selected: boolean;
}
