export interface PictureInfo {
  id: string;
  picUrl: string;
  title: string;
}

export interface PictureInfoVm {
  id: string;
  picUrl: string;
  title: string;
  selected: boolean;
}

export interface PictureInfoVm {
  id: string;
  picUrl: string;
  title: string;
  selected: boolean;
}
